# Copy to .env for local development (optional; python-dotenv can load it if you add load_dotenv to wsgi)
SECRET_KEY=change-me-to-a-long-random-string
DATABASE_URL=postgresql://user:pass@host:5432/dbname
# Local SQLite fallback is used if DATABASE_URL is unset:
# DATABASE_URL=sqlite:////tmp/medaniyetler_finance.db
